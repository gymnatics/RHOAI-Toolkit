apiVersion: v1
kind: Service
metadata:
  name: qwen3-4b-external
  namespace: admin-workshop
spec:
  selector:
    app: isvc.qwen3-4b-predictor
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
