apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: admin-workshop
  name: secret-reader
  labels:
    purpose: workshop-prometheus-access
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list"]
