apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
  - name: workshop-users
    mappingMethod: claim
    type: HTPasswd
    htpasswd:
      fileData:
        name: workshop-htpasswd-secret
