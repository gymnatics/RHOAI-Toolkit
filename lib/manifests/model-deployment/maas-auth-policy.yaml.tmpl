---
apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSAuthPolicy
metadata:
  name: ${POLICY_NAME}
  namespace: models-as-a-service
spec:
  modelRefs:
    - name: ${MODEL_NAME}
      namespace: ${NAMESPACE}
  subjects:
    groups:
      - name: ${MAAS_GROUP}
