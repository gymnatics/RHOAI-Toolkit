apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm

# NOTE: model.yaml contains ${DISCONNECTED_REGISTRY} envsubst placeholders.
# Run envsubst on a temp copy before `oc apply -k` (scripts/deploy-maas-model.sh
# does this automatically for --model simulator-disconnected).
resources:
  - model.yaml
