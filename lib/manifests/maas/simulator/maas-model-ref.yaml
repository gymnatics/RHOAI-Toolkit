# Registers the simulator model with the MaaS control plane
---
apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSModelRef
metadata:
  name: simulator
  namespace: ${MODEL_NAMESPACE}
spec:
  llmInferenceServiceRef:
    name: simulator
