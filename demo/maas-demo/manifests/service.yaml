apiVersion: v1
kind: Service
metadata:
  name: maas-demo
  labels:
    app: maas-demo
    app.kubernetes.io/name: maas-demo
    app.kubernetes.io/component: streamlit-app
spec:
  selector:
    app: maas-demo
  ports:
  - port: 8501
    targetPort: 8501
    name: http
